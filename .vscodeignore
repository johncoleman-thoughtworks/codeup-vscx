# Source — only compiled output ships
src/**
**/*.ts
**/*.map
tsconfig.json

# Dev-only state
.vscode/**
.vscode-test/**
.github/**
.gitignore
.codeup/**
.idea/**
.claude/**
codeup.iml
SECURITY.md

# Test artifacts
dist/test/**

# Misc
.DS_Store
*.vsix
PLAN.md
TESTING.md
codeup-vscx-findings-*.md

# vsce walks package.json dependencies itself — don't fight it with manual
# node_modules excludes. resources/ ships by default (no exclusion).
